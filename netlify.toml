[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Skip installing Python dependencies if not needed for the frontend build
[build.environment.PYTHON_REQUIREMENTS]
  install = "false"